// src/utils.ts
var checkEnvs = (keys) => {
  if (keys.length == 0)
    return void 0;
  for (let i = 0; i < keys.length; i++) {
    const key = keys[i];
    if (process.env[key] != void 0)
      continue;
    else
      return key;
  }
  return void 0;
};
export {
  checkEnvs
};
